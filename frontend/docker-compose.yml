version: '3.8'

services:
  todo-frontend:
    build: .
    container_name: todo-frontend
    ports:
      - "8080:80"
    restart: unless-stopped
    environment:
      - NGINX_HOST=localhost
      - NGINX_PORT=80
